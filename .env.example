# Gateway server port
PORT=4000

# Allowed CORS origin (the frontend URL)
CLIENT_ORIGIN=http://localhost:5173

# Docker daemon socket path
DOCKER_SOCKET=/var/run/docker.sock

# Docker image used to spin up sandbox containers
SANDBOX_IMAGE=subterm-server
